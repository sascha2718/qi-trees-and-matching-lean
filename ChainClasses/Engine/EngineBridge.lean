import ChainClasses.Engine.BushyProfileBridge

/-! The bushy matching interface uses arbitrary bounded binary profiles and the stopped
Markov theorem. Its public matching declaration is `ChainClasses.exists_profile_match`.
The finite-law and geometric interfaces are in `ProfileLaw`, `ProfileProjection`,
`ProfileMatching` and `ProfileGeometry`. -/
